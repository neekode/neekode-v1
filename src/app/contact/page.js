export const metadata = {
  title: 'contact'
};

// { children }
export default function Contact() {
  return (
    <div
      id="contact"
      className="page"
    >
      contact
    </div>
  );
}
