export const metadata = {
  title: 'projects'
};

// { children }
export default function Projects() {
  return (
    <div
      id="projects"
      className="page"
    >
      projects
    </div>
  );
}
