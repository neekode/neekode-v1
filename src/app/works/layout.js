export const metadata = {
  title: 'works'
};

export default function Works({ children }) {
  return (
    <div
      id="works-layout"
      className="page"
    >
      works layout
      { children }
    </div>
  );
}
